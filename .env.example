# Star India Frontend Environment Configuration Example
# Leave blank in development to route requests through Vite's proxy (no CORS issues)
# In production, specify https://starindia.life if hosted separately

VITE_API_BASE_URL=
